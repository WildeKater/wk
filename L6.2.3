/*
 *3)Существует массив {1,2,3,4,5} — размер массива может быть
 *произвольным. Напишите программу которая выведет на экран все
 *возможные комбинации из этих цифр. Внимание повторений быть не
 *должно.(2 часа)
 */

package com.gmail.wildekatertz;

import java.util.Arrays;

public class Main {

	public static void main(String[] args) {

		int[] mas = { 1, 2, 3, 4, 5 };

		int count = factorial(mas.length);

		int max = mas.length - 1;

		int temp;
		int i = max;
		while (count > 0) {

			temp = mas[i];
			mas[i] = mas[i - 1];
			mas[i - 1] = temp;

			System.out.println(Arrays.toString(mas));

			count--;

			if (i < 2) {
				i = max;
			} else {
				i--;
			}
		}

		System.out.println("Всего " + factorial(mas.length) + " вариантов перестановок");

	}

	static int factorial(int n) {
		int fact = 1;
		for (int i = 1; i <= n; i++) {
			fact = fact * i;
		}
		return fact;
	}

}
