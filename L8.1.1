/*
 *1) Создайте консольный «текстовый редактор» с возможностью
 *сохранения набранного текста в файл.
 */

package com.gmail.wildekatertz;

import java.io.IOException;
import java.io.File;
import java.io.FileNotFoundException;
import java.io.PrintWriter;
import java.util.Scanner;

public class Main {

	public static void main(String[] args) {

		Scanner sc = new Scanner(System.in);

		System.out.println("Введите адрес каталога");
		String dirPath = sc.nextLine();

		System.out.println("Введите название файла");
		String fileName = sc.nextLine();

		System.out.println("Введите текст");
		String text = sc.nextLine();

		String filePath = dirPath + fileName;

		File newFile = new File(filePath);

		try {
			newFile.createNewFile();
		} catch (IOException e) {

		}
		try (PrintWriter newPrint = new PrintWriter(newFile)) {
			newPrint.println(text);
		} catch (FileNotFoundException e) {
			System.out.println("Ошибка записи в файл " + filePath);
		}

	}

}
